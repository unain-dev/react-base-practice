* ts에게 react component를 어떻게 설명하는지
* component를 type한다 = component에게 type을 준다. =  ts에게 뭐가 뭔지 설명해준다.
* 어떻게 component가 필요로 하는 prop을 ts에게 설명할 수 있는지

- prop types : prop이 거기 있는지 아닌지 판단. 하지만, 코드를 실행 한 후에만 확인이 가능.
    -> 우리가 ts를 사용하는 이유는 코드 실행 전에 잘못된 것을 알기 위함임. 그래서 prop types를 사용하지 않음!
    -> 대신 prop들을 ts로 보호해줄 것임.
- 