function Movie({ id, title, rating }) {
  return (
    <div>
      <p>id : {id}</p>
      <p>title : {title}</p>
      <p>rating : {rating}</p>
      <hr></hr>
    </div>
  );
}

export default Movie;
